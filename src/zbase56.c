#include "zbase56.h"

static const char zbase56_alphabet[] = {
	'y', 'b', 'n', 'd', 'r', 'f', 'g', '8',
	'e', 'j', 'k', 'm', 'c', 'p', 'q', 'x',
	'o', 't', '1', 'u', 'w', 'i', 's', 'z',
	'a', '3', '4', '5', 'h', '7', '6', '9',
	'Y', 'B', 'N', 'D', 'R', 'F', 'G', 'E',
    'J', 'K', 'M', 'C', 'P', 'Q', 'X', 'O',
    'T', 'U', 'W', 'I', 'S', 'Z', 'A', 'H'
};

int zbase56_dec(const uint8_t *input, uint8_t *output, int outputLen) {
    return -1;
}
int zbase56_enc(const uint8_t *input, int len, uint8_t *output, int outputLen) {
	/*do {*/
		/*printf("%c", zbase32_alphabet[number % 32]);*/
		/*number /= 32;*/
	/*} while (number > 0);*/
    return -1;
}